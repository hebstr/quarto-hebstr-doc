default:
  depth: 1
  highlight:
    - "^README%.md$"

    # comment
    - "^src$"
  paths:
    "README.md": "default desc"

doc:
  depth: 3
  exclude:
    - "^README%.md$"
  paths:
    "src/main.lua": "doc desc"

scalar: true

disabled: false
